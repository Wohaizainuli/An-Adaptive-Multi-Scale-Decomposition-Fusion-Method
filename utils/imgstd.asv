function re=imgstd(img, siz)
if nargin < 1 || nargin > 2
    error('The input is wrong...');
end
if nargin < 2
    siz=5;
end
if size(img,3) ~= 1
    img=rgb2gray(img);
end
img=im2double(img);
[c,r]=size(img);
for i=1:siz:c-siz
    for j=1:siz:r-siz
        temp=img(i:i+siz-1,j:j+siz-1);

    end
end
end